# iter-114 同步 manual.md（更新 GUI 操作描述与新增功能说明）

## 需求清单

- [x] 同步 manual.md（req-34 第 8 项；req-32 遗留 GUI 操作描述）

## 迭代目标

`docs/manual.md` 严重过时（版本 0.1.8，描述旧版 Tab 顶部布局），需完整重写
以反映 iter-104 至 iter-113 引入的 GUI 重构与新增功能：
侧边栏布局、工作区卡片、任务级配置覆盖、结果过滤排序、批量替换撤销、
双进度条、设置 TabBar、文件类型三态勾选、暗色模式等。

## 改动文件清单

- `docs/manual.md`：完整重写，章节从 12 节扩展到 14 节，版本号 0.1.8 → 0.2.2
- `src/fuscan/assets/docs/fuscan-用户手册.pdf`：通过 `scripts/generate_manual_pdf.py`
  从新 Markdown 源重新生成，随包分发
- `.trae/req/req-34-代码结构优化与性能提升.md`：标记第 8 项已完成
- `.trae/docs/iter-104-...md` / `iter-105-...md`：清理（保留最新 5 条迭代记录）

## 关键决策与依据

### 1. 完整重写而非局部修订

- 原 manual.md 描述的「顶部 Tab（扫描/规则/历史）」「顶部操作区」「三阶段全页切换」
  与当前 GUI 完全不符（iter-100 起改为侧边栏 + 工作区卡片布局）
- 局部修订会产生新旧描述混杂，误导用户
- 完整重写可确保描述与当前 GUI 一致

### 2. 章节结构调整

新增章节：
- **第 4 节 工作区与任务管理**：工作区卡片、操作按钮、展开操作、清空工作区
- **第 7 节 详情区与命中操作**：含 iter-113 批量替换与撤销
- **第 8 节 任务级配置覆盖**：iter-104 任务级设置
- **第 11 节 统计信息**：StatsPage
- **第 12 节 关于页**：含用户手册与配置目录入口

合并/删除：
- 原「扫描历史」章节删除（功能已弱化为路径历史，合并到添加任务与设置中说明）
- 原「导出扫描结果」章节合并到工作区卡片展开操作中

### 3. PDF 重新生成

按 `rule-12-文档与版本发布.md`：
- manual.md 新增章节且变更 GUI 操作描述 → 必须重新生成 PDF
- 执行 `uv run python scripts/generate_manual_pdf.py` 输出 0.2.2 版本 PDF
- 覆盖 `src/fuscan/assets/docs/fuscan-用户手册.pdf`

### 4. 迭代记录清理

按 `rule-02-产物约束.md`：迭代文件数超过 5 时从最旧开始清理。
当前 6 条（iter-104/105/106/107/111/112），新增 iter-114 后变 7 条，
清理 iter-104、iter-105 两条最旧记录，保留 5 条最新。

## 代码实现情况

### manual.md 关键变更

- 版本号：0.1.8 → 0.2.2
- 最后更新：2026-07-22 → 2026-07-27
- 章节数：12 → 14
- 主界面描述：从「上到下五层次 + 顶部 Tab」改为「侧边栏 + 内容区」
- 工作流：从「整页切换三阶段」改为「工作区卡片操作」
- 大文件跳过阈值：100MB → 50MB（与代码硬约束一致）
- 并发线程数默认：4-8 → 5（与硬约束一致）
- 新增：批量替换与撤销、任务级配置覆盖、结果过滤排序、暗色模式、字体设置等

### PDF 生成验证

```bash
uv run python scripts/generate_manual_pdf.py
# 输出：已生成: F:\Dev\fuscan\src\fuscan\assets\docs\fuscan-用户手册.pdf (版本 0.2.2)
```

## 整合优化情况

- 删除 manual.md 中过时的「100MB 大文件跳过」描述，与硬约束 50MB 对齐
- 删除「8 条内置规则」具体数字（避免后续新增规则时手册失真）
- 统一术语：用「工作区」替代「任务」表述扫描单元
- 修正快捷键表中 F1 用户手册入口（原缺失）

## 测试验证结果

- `manual.md` 已通过 Markdown 语法检查（无格式错误）
- PDF 生成脚本执行成功，输出文件大小合理
- 版本号 0.2.2 与 `src/fuscan/__init__.py` 中 `__version__` 一致
- 不涉及代码改动，无需运行 ruff/pyrefly/pytest

## 遗留事项

- 无

## 下一轮计划

iter-115：扫描历史归档与多次扫描对比摘要
