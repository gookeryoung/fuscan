# 哈希缓存集成 - 收尾实施计划

## 摘要

为 fuscan 完成哈希缓存全链路集成的收尾工作。缓存子包（`fuscan.cache`）、Scanner 缓存集成、ArchiveScanner 缓存集成、IncrementalScanner 委托改造及对应测试**已完成**。本计划覆盖剩余接入工作：TrayApp 接入 cache、修复 test_tray.py 失败测试、CLI 接入缓存选项与 cache 子命令、GUI 集成缓存（ScanWorker/MainWindow/SettingsDialog）、配套测试与文档。

## 当前状态分析

### 已完成（前期工作）

| 模块 | 文件 | 状态 |
|------|------|------|
| 缓存子包 | `src/fuscan/cache/{__init__,schema,hashes,store}.py` | 完整实现，6 张表，56 测试通过 |
| Scanner 集成 | `src/fuscan/scanner/scanner.py` | `__init__` 有 `cache`/`source_files` 参数；`_scan_entry_cached`/`_scan_entry_uncached` 分流；`default_extract_content_with_hash` |
| ArchiveScanner 集成 | `src/fuscan/archive/scanner.py` | `__init__` 有 `cache` 参数；分流已实现 |
| IncrementalScanner | `src/fuscan/watcher/incremental.py` | 委托 Scanner + cache，`save_state`/`load_state`/`mark_scanned`/`remove_path` 为空操作 |
| Config 字段 | `src/fuscan/config.py` | `cache_enabled: bool = True`、`cache_path: str | None = None` |
| Scanner 测试 | `tests/test_scanner.py` `TestScannerCache` | 11 个测试方法 |
| Archive 测试 | `tests/test_archive.py` `TestArchiveScannerCache` | 6 个测试方法 |
| test_watcher.py | `tests/test_watcher.py` | `TestIncrementalScanner`（无 cache，9 测试）+ `TestIncrementalScannerCache`（有 cache，6 测试）已完成 |

### 待完成（本计划范围）

| 任务 | 文件 | 目标 |
|------|------|------|
| 8c | `src/fuscan/watcher/tray.py` | `__init__` 加 `cache` 参数，传给 IncrementalScanner；`_quit` 关闭 cache |
| 8d | `tests/test_tray.py` | 修复 3 个失败测试（state 持久化相关） |
| 9 | `src/fuscan/cli.py` | `scan` 加 `--no-cache`/`--cache-path`；新增 `cache` 子命令；`_cmd_scan`/`_cmd_tray` 构造 CacheStore |
| 9t | `tests/test_cli.py` | 新增 `TestCacheCommand` 测试类 |
| 10a | `src/fuscan/gui/worker.py` | `__init__` 加 `cache`/`source_files` 参数，传给 Scanner |
| 10b | `src/fuscan/gui/main_window.py` | 构造/持有 CacheStore，传给 ScanWorker，closeEvent 关闭 |
| 10c | `src/fuscan/gui/settings_dialog.py` | 扫描设置页加缓存设置分组 |
| 10t | `tests/test_gui.py` | 新增缓存相关测试 |
| S | `src/fuscan/cache/sources.py`（新增） | `compute_source_files` 公共辅助函数 |
| 12 | `.trae/docs/iter-01-哈希缓存.md`、`.trae/req/req-01-哈希缓存.md` | 迭代记录与需求记录 |

## 假设与决策

1. **覆盖率门槛 96%**：`pyproject.toml` 已配置 `fail_under=96`，所有变更不得降低覆盖率。
2. **默认启用缓存**：`Config.cache_enabled=True`，`cache_path=None` 表示默认路径 `~/.fuscan/cache.db`。
3. **`compute_source_files` 公共函数**：放 `src/fuscan/cache/sources.py`，供 CLI 和 GUI 共用。惰性导入 `fuscan.builtin` 避免循环依赖。
4. **CacheStore 生命周期**：CLI 用 `with CacheStore(...)` 上下文管理；GUI 在 MainWindow 持有，closeEvent 关闭；Tray 在 TrayApp 持有，_quit 关闭。
5. **cache 子命令**：`fuscan cache stats` 显示统计，`fuscan cache clear` 清空缓存（删除数据库文件），`fuscan cache prune [--max-age-days N]` 清理过期。
6. **test_tray.py 失败测试处理**：3 个 state 持久化测试（`test_construct_loads_state_file`/`test_handle_scan_result_persists_state`/`test_quit_persists_state`）因 save_state/load_state 变为空操作而失败。重写为 cache 行为测试或删除。
7. **Python 3.8 兼容**：`from __future__ import annotations`；`CacheStore` 导入放 `TYPE_CHECKING` 块。

## 变更详情

### 任务 S：新增 `compute_source_files` 公共函数

**文件**：`src/fuscan/cache/sources.py`（新建）

**原因**：CLI 和 GUI 都需要计算规则文件 SHA-256 映射，传给 `Scanner.__init__` 的 `source_files` 参数。提取为公共函数避免重复。

**内容**：

```python
"""规则来源文件哈希计算工具。"""
from __future__ import annotations
from pathlib import Path
from fuscan.cache.hashes import hash_bytes

__all__ = ["compute_source_files"]

def compute_source_files(rules_paths: list[Path], use_builtin: bool) -> dict[Path, str]:
    """计算规则文件 SHA-256 映射，供 CacheStore.register_ruleset 使用。"""
    sources: dict[Path, str] = {}
    if use_builtin:
        from fuscan.builtin import BUILTIN_RULES_PATH
        if BUILTIN_RULES_PATH.exists():
            sources[BUILTIN_RULES_PATH] = hash_bytes(BUILTIN_RULES_PATH.read_bytes())
    for p in rules_paths:
        if p.exists():
            sources[p] = hash_bytes(p.read_bytes())
    return sources
```

**`src/fuscan/cache/__init__.py` 变更**：导出 `compute_source_files`。

### 任务 8c：TrayApp 接入 cache 参数

**文件**：`src/fuscan/watcher/tray.py`

**变更**：
1. `__init__` 加 `cache: CacheStore | None = None` 参数（加到现有参数末尾，`parent` 之前）
2. 构造 `IncrementalScanner` 时传 `cache=cache`
3. 存 `self._cache = cache` 供 `_quit` 关闭
4. `_quit` 中在 `QApplication.quit()` 前调用 `self._cache.close()`（如果 cache 不为 None）

**导入**：`CacheStore` 放 `TYPE_CHECKING` 块（已存在 `MainWindow`/`ScanWorker` 的 TYPE_CHECKING 导入）。

### 任务 8d：修复 test_tray.py 失败测试

**文件**：`tests/test_tray.py`

**3 个失败测试及处理**：

1. **`test_construct_loads_state_file`（行 80-89）**：重写为 cache 行为测试
   - 构造 CacheStore，传给 IncrementalScanner，扫描文件
   - 用同一 CacheStore 构造 TrayApp，验证 `tracked_count` 反映缓存

2. **`test_handle_scan_result_persists_state`（行 286-311）**：重写
   - 传 cache 给 TrayApp，执行 `_handle_scan_result`
   - 验证 cache.stats() 反映扫描结果（而非验证 state_file 存在）

3. **`test_quit_persists_state`（行 366-375）**：重写
   - 传 cache 给 TrayApp，调用 `_quit`
   - 验证 cache 被关闭（不抛异常），cache.db 文件存在

### 任务 9：CLI 接入缓存

**文件**：`src/fuscan/cli.py`

**变更内容**：

1. **`build_parser` 中 `scan_parser` 新增参数**：
   - `--no-cache`：`store_true`，默认 False，禁用缓存
   - `--cache-path PATH`：`type=Path, default=None`，自定义缓存路径

2. **新增 `cache` 子命令**（在 `subparsers` 中）：
   ```
   fuscan cache stats              # 显示缓存统计
   fuscan cache clear              # 清空缓存（删除数据库文件）
   fuscan cache prune [--max-age-days N]  # 清理过期文件缓存（默认 30 天）
   ```

3. **`_cmd_scan` 变更**：
   - 根据 `args.no_cache`（反转）和 `config.cache_enabled` 决定是否启用缓存
   - 启用时构造 `CacheStore`（`args.cache_path` 或 `config.cache_path` 或默认路径）
   - 用 `with CacheStore(...)` 管理生命周期
   - 调用 `compute_source_files` 计算 source_files
   - 传 `cache` 和 `source_files` 给 `Scanner`

4. **`_cmd_tray` 变更**：
   - 构造 `CacheStore`（若 `config.cache_enabled`）
   - 传给 `TrayApp(cache=cache)`

5. **新增 `_cmd_cache`**：处理 `cache` 子命令的 `stats`/`clear`/`prune`。
   - `stats`：调用 `CacheStore.stats()`，打印统计信息到 stdout
   - `clear`：关闭 CacheStore（如有），删除数据库文件（含 -wal/-shm）
   - `prune`：调用 `prune_stale_files(max_age_days)`，打印清理数量

6. **`main()` 变更**：加 `if args.command == "cache": return _cmd_cache(args)` 分支。

### 任务 9t：CLI 缓存测试

**文件**：`tests/test_cli.py`

**新增 `TestCacheCommand` 测试类**：

- `test_cache_stats_shows_statistics`：`cache stats` 输出统计信息
- `test_cache_clear_deletes_db`：`cache clear` 删除数据库文件
- `test_cache_prune_removes_stale`：`cache prune --max-age-days 0` 清理过期
- `test_scan_no_cache_flag_disables_cache`：`--no-cache` 时不创建 cache.db
- `test_scan_cache_path_flag_uses_custom_path`：`--cache-path` 使用自定义路径
- `test_scan_creates_cache_db`：默认 scan 后 cache.db 存在

### 任务 10a：ScanWorker 加 cache 参数

**文件**：`src/fuscan/gui/worker.py`

**变更**：
1. `__init__` 加 `cache: CacheStore | None = None` 和 `source_files: Mapping[Path, str] | None = None` 参数
2. 存为 `self._cache` 和 `self._source_files`
3. `run()` 中构造 `Scanner` 时传 `cache=self._cache` 和 `source_files=self._source_files`

**导入**：`CacheStore` 和 `Mapping` 放 `TYPE_CHECKING` 块。

### 任务 10b：MainWindow 管理 CacheStore 生命周期

**文件**：`src/fuscan/gui/main_window.py`

**变更**：
1. `__init__` 中根据 `self._config.cache_enabled` 构造 `CacheStore`：
   - 路径：`Path(config.cache_path)` 或 `default_cache_path()`
   - 存为 `self._cache_store: CacheStore | None`
2. `_on_scan` 中构造 `ScanWorker` 时传 `cache=self._cache_store` 和 `source_files=compute_source_files(self._rules_paths, self._use_builtin)`
3. `closeEvent` 中关闭 `self._cache_store`（如果不为 None）

**导入**：`from fuscan.cache import CacheStore, default_cache_path, compute_source_files`。

### 任务 10c：SettingsDialog 加缓存设置 UI

**文件**：`src/fuscan/gui/settings_dialog.py`

**变更**：
1. `_build_scan_settings_page` 中新增 "缓存设置" QGroupBox：
   - `QCheckBox` "启用扫描结果缓存（推荐）" → `self._cache_enabled_check`
   - `QLineEdit` 缓存数据库路径 + 浏览按钮 → `self._cache_path_edit` + `self._cache_path_browse_btn`
2. `_load_config`：读取 `cache_enabled`/`cache_path`
3. `_save_config`：写入 `cache_enabled`/`cache_path`

**导入**：需加 `QLineEdit`、`QPushButton`、`QFileDialog` 到 PySide 导入块。

### 任务 10t：GUI 缓存测试

**文件**：`tests/test_gui.py`

**新增测试**（加到 `TestScanWorker` 和 `TestSettingsDialog` 类或新增类）：
- `test_scan_worker_with_cache`：ScanWorker 传 cache 后扫描结果正确
- `test_main_window_creates_cache`：MainWindow 初始化时根据 config 创建 CacheStore
- `test_main_window_cache_disabled`：cache_enabled=False 时不创建 CacheStore
- `test_settings_dialog_cache_options`：设置对话框缓存选项读写

### 任务 12：文档

**文件**：
- `.trae/docs/iter-01-哈希缓存.md`：迭代记录（目标、改动文件清单、关键决策、验证结果、遗留事项）
- `.trae/req/req-01-哈希缓存.md`：需求记录（按 `[] 需求描述` 格式）

## 验证步骤

1. **ruff**：`uv run ruff check src tests && uv run ruff format --check src tests`
2. **pyrefly**：`uv run pyrefly check`
3. **pytest**：`uv run pytest -m "not slow" --cov=fuscan --cov-fail-under=96`
4. **全量测试通过**：包括 test_watcher.py、test_tray.py、test_cli.py、test_gui.py、test_cache.py、test_scanner.py、test_archive.py

## 执行顺序

1. 任务 S（`compute_source_files` 公共函数）
2. 任务 8c（TrayApp 接入 cache）
3. 任务 8d（修复 test_tray.py）
4. 任务 9（CLI 接入 + cache 子命令）
5. 任务 9t（CLI 测试）
6. 任务 10a（ScanWorker）
7. 任务 10b（MainWindow）
8. 任务 10c（SettingsDialog）
9. 任务 10t（GUI 测试）
10. 任务 12（文档）
11. 最终验证（ruff + pyrefly + pytest --cov）
